@model List<Practica.EF.MVC.Models.CategoriesView>

@{
    ViewBag.Title = "Categories List";
}


<h1>List of Categories</h1>

@Html.ActionLink("Insert new Category", "Insert", new { id = 0 }, new { @class = "btn button" })
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Name</th>
            <th scope="col">Description</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.Name</td>
                <td>@category.Description</td>
                <td>
                    @Html.ActionLink("Update", "Insert", new { id = category.Id }, new { @class = "btn button" })
                    @Html.ActionLink("Delete", "Delete", new { id = category.Id }, new { @class = "btn btn-danger", onclick = "return confirm('Are you sure you wanna delete this category?')" })
                </td>
            </tr>
        }
    </tbody>
</table>



